{%- comment -%}
  Sello circular de Dulcey Elixir (monograma "DE" + aro dorado, versión fondo
  oscuro). Es una imagen, no SVG inline con currentColor, porque el trazo
  caligráfico usa un degradado dorado fijo que no se controla con `fill`.
{%- endcomment -%}
<img
  class="logo__mark"
  src="{{ 'dulcey-logo-circular.svg' | asset_url }}"
  width="40" height="40"
  alt=""
  loading="lazy"
>
